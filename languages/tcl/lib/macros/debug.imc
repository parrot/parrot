# It would be nice if we could print out arbitrary strings and have everything
# automagically prefixed with "DEBUG: "

# It'd be nice if we could take varargs, ala:

# _debug("there are ", $I0, " items");
# not sure how hard calling conventions'd make this.

.macro debug(MESSAGE)
   
  if debug != 1 goto .$DONE
  print .MESSAGE 

.local $DONE:

.endm
