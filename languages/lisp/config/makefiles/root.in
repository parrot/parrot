RM_F = @rm_f@
PERL = @perl@
PARROT = ../../parrot@exe@

all: build

test: build
	$(PARROT) -r lisp.pbc t/test.l

build: lisp.pir
	$(PARROT) -o lisp.pbc lisp.pir

clean:
	$(RM_F) core "*.pbc" "*~"

realclean: clean
	$(RM_F) Makefile
