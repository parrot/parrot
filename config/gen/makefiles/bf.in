RM_F = ${rm_f}
PERL = ${perl}

ASSEMBLE=$(PERL) ../../assemble.pl
PARROT=../../parrot

all: build

test: build
	$(PARROT) bf.pbc test.bf

build: bf.pasm
	$(ASSEMBLE) bf.pasm > bf.pbc

clean:
	$(RM_F) core *.pbc *~
