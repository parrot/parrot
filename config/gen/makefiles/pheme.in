# $Id: /parrotcode/trunk/config/gen/makefiles/pheme.in 13083 2006-03-04T20:17:22.853686Z svm  $

# Setup some commands
PERL     = @perl@
RM_F     = @rm_f@
PARROT   = ../../parrot@exe@
PGE_DIR  = ../../compilers/pge
CP       = @cp@

# the default target
all: pheme.pbc

lib/PhemeGrammar.pbc: lib/PhemeGrammar.pir lib/pheme_grammar_gen.pir
	$(PARROT) -o lib/PhemeGrammar.pbc --output-pbc lib/PhemeGrammar.pir

lib/pheme_grammar_gen.pir: lib/pheme.g
	$(PARROT) $(PGE_DIR)/pgc.pir --output=lib/pheme_grammar_gen.pir lib/pheme.g

lib/PhemeSymbols.pbc: lib/PhemeSymbols.pir
	$(PARROT) -o lib/PhemeSymbols.pbc --output-pbc lib/PhemeSymbols.pir

lib/PhemeCompiler.pbc: lib/PhemeCompiler.pir lib/POST.pbc
	$(PARROT) -o lib/PhemeCompiler.pbc --output-pbc lib/PhemeCompiler.pir

lib/POST.pbc: lib/POST.pir
	$(PARROT) -o lib/POST.pbc --output-pbc lib/POST.pir

pheme.pbc: lib/PhemeGrammar.pbc pheme.pir lib/PhemeSymbols.pbc lib/PhemeCompiler.pbc
	$(PARROT) -o pheme.pbc --output-pbc pheme.pir

# This is a listing of all targets that users can call
help:
	@echo ""
	@echo "Following targets are available for the user:"
	@echo ""
	@echo "  all:               pheme.pbc"
	@echo "                     This is the default."
	@echo "Testing:"
	@echo "  test:              Run the test suite."
	@echo "  testclean:         Clean up test results."
	@echo ""
	@echo "Cleaning:"
	@echo "  clean:             Basic cleaning up."
	@echo "  realclean:         Removes also files generated by 'Configure.pl'"
	@echo "  distclean:         Removes also anything built, in theory"
	@echo ""
	@echo "Misc:"
	@echo "  help:              Print this help message."
	@echo ""

test: all
	$(PERL) -Ilib t/harness

testclean:
	$(RM_F) "t/*.out"

clean: testclean
	$(RM_F) pheme.pbc
	$(RM_F) lib/pheme_grammar_gen.pir
	$(RM_F) lib/*.pbc

realclean: clean

distclean: realclean
